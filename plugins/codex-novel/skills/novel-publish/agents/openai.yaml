interface:
  display_name: "Novel Publish"
  short_description: "Surface, approve, publish, and recover scenes"
  default_prompt: "Use $novel-publish to prepare and inspect an exact scene publication, then ask for its digest approval now."

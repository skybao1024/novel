interface:
  display_name: "Novel Publish"
  short_description: "Approve resolved scene traces and publish safely"
  default_prompt: "Use $novel-publish to prepare and inspect an exact Scene publication with its resolved Scene Trace, then ask for digest approval."

policy:
  allow_implicit_invocation: true

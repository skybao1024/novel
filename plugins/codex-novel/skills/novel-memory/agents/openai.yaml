interface:
  display_name: "Novel Memory"
  short_description: "Navigate summaries, entity lines, and exact prose"
  default_prompt: "Use $novel-memory to locate relevant summaries and Entity occurrence lines, then read exact approved historical prose."

policy:
  allow_implicit_invocation: true

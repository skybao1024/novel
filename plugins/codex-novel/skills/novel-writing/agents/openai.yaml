interface:
  display_name: "Novel Writing"
  short_description: "Write clear, emotionally alive Novel Chapters"
  default_prompt: "Use $novel-writing to recover exact history, confirm a concise causal-and-emotional Chapter plan, write clear and emotionally alive viewpoint prose with meaningful setting, review the exact Draft, show the exact ready revision and wait for author confirmation, then prepare publication digest approval while preserving separate confirmation and publication boundaries."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Novel Writing"
  short_description: "Write with continuity and resolved entity traces"
  default_prompt: "Use $novel-writing to recover required predecessor-Chapter prose, preserve the exact new-Chapter heading, resolve Entity mentions into a Scene Trace, review, and prepare immediate digest approval."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "Novel Bootstrap"
  short_description: "Create contained projects and approve novel intent"
  default_prompt: "Use $novel-bootstrap to create a contained local novel project, calibrate an author-approved Voice Contract with clear action, lived viewpoint, emotional movement, meaningful setting, and positive prose anchors, then prepare its exact Intent Diff for approval."
